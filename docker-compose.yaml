services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: 3d1-web
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./public/thumbnail:/app/public/thumbnail
    env_file:
      - .env
    restart: unless-stopped
